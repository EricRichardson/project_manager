<%= bootstrap_form_for([@project, @task]) do |f| %>

    <%= f.text_field :title %>

    <%= f.date_select :duedate %>

  <% unless @task.title == nil %>
      <%= f.check_box :completed %>
  <% end %>
  
  <%= f.submit %>
<% end %>
