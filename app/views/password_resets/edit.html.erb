<%= bootstrap_form_tag url: password_reset_path, method: :patch do |f| %>
  <%= hidden_field_tag :token, @token %>

  <%= f.password_field :new_password %>

  <%= f.password_field :new_password_confirmation %>

  <%= f.submit "Generate New Password" %>
<% end %>
